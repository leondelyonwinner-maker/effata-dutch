# Copy to .env for local development. Never commit the real .env file.

ENVIRONMENT=development

# Local dev defaults to SQLite; Render injects DATABASE_URL for Postgres in production.
DATABASE_URL=sqlite+aiosqlite:///./effata_dutch.db

# User accounts (per-learner passcode hashes) live in the `users` table, not
# here. After `python -m app.seed`, create one account per learner with:
#   python -m app.cli create-user <username> "<Display Name>" "<passcode>"

# Generate with: python -c "import secrets; print(secrets.token_urlsafe(48))"
SESSION_SECRET_KEY=

DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat
